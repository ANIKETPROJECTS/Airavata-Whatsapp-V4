I'm debugging my WhatsApp Embedded Signup integration (Meta/Facebook Embedded Signup for WhatsApp Business Platform). I need to verify whether my system is correctly capturing the `waba_id` and `phone_number_id` when a customer completes the signup flow.

Please do the following:

1. Search my entire codebase for any FB.login() call and show me the full config being passed to it (specifically check if config_id, response_type, and override_default_response_type are set correctly).

2. Search for any `window.addEventListener('message', ...)` handler related to Embedded Signup. If it doesn't exist, tell me clearly that it's missing — this is required to capture the WA_EMBEDDED_SIGNUP postMessage event that Meta sends with { data: { phone_number_id, waba_id } } on FINISH.

3. If the listener exists, check:
   - Is it verifying event.origin is from a Meta domain (facebook.com) before processing the message, for security?
   - Is it correctly parsing event.data as JSON if needed?
   - Is it checking for data.type === 'WA_EMBEDDED_SIGNUP' and data.event === 'FINISH'?
   - Where is it storing the captured waba_id and phone_number_id — is it saving to my database, or just logging to console and getting lost?

4. Search my backend/API routes for where the FB.login() 'code' callback is sent, and confirm whether the code-to-token exchange is actually happening, and whether the resulting access token is linked to the correct customer record along with their waba_id and phone_number_id.

5. Check my database schema/models to confirm there are actual columns/fields for storing waba_id and phone_number_id per customer, and that the save/insert logic after signup completion is actually being called.

6. Add temporary console.log or server-side logging at each of these points (postMessage received, code received, token exchange response, DB save) so I can trace exactly where the data is or isn't flowing when I test the flow live.

7. Summarize for me: at which exact step (if any) the waba_id/phone_number_id capture is broken or missing.

Do not just explain the theory — actually search and show me the relevant file names, line numbers, and code snippets from my project.