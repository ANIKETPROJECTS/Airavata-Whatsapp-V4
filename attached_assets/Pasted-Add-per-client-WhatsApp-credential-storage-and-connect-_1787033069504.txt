Add per-client WhatsApp credential storage and connect it to message 
sending. This is a MongoDB (Mongoose) app. Do not touch unrelated 
collections or files.

CONTEXT — my current schema:
- users collection already has: metaPhoneNumberId, metaWabaConnected 
  (boolean), creditBalance
- apikeys collection is unrelated — it stores bcrypt-hashed platform 
  API keys, not WhatsApp credentials. Do not modify it.
- attributes collection is unrelated — empty custom-fields array. 
  Do not modify it.
- There is currently NO field storing the actual Meta WhatsApp access 
  token anywhere.

1. NEW COLLECTION: whatsappcredentials
   Fields: userId (ref to users._id), wabaId, phoneNumberId, 
   accessTokenEncrypted, createdAt, updatedAt
   - accessTokenEncrypted must use reversible AES-256 encryption 
     (NOT bcrypt/hashing — we need to decrypt and reuse this token 
     on every outbound message, unlike the one-way apikeys pattern)
   - Store the encryption key in an environment variable, never in 
     code or the database

2. CONNECT FLOW
   - Wherever the "Connect WhatsApp" action currently sets 
     metaWabaConnected: true, also write the wabaId, phoneNumberId, 
     and encrypted accessToken into whatsappcredentials for that 
     userId
   - If this flow doesn't exist yet, tell me before building it — 
     don't assume the UI/API shape

3. MESSAGE SENDING
   - Find the existing code path that sends WhatsApp messages 
     (chatbot flows, templates, campaigns)
   - Update it to look up the sending user's credentials from 
     whatsappcredentials by userId, decrypt the token, and use that 
     instead of any shared/hardcoded credential
   - If messages are currently sent using a single shared token 
     (env variable or similar), point that out to me explicitly 
     before changing it

4. WEBHOOK ROUTING
   - Incoming WhatsApp webhook events include phone_number_id — 
     use it to look up the matching document in whatsappcredentials, 
     get the userId, and route the event to that user's chatbot flow

Do not modify chatbotflows, templates, campaigns UI logic, or the 
apikeys/attributes collections. Do not add new npm dependencies 
without asking me first — for AES-256, use Node's built-in crypto 
module unless you tell me why something else is needed. 
Stop and ask me before making any change item 2, 3, or 4 depends on 
if you can't find the relevant existing code.